# Not copied into the build context (comments on their own lines!)

# installed fresh inside the image
node_modules

# build output — regenerated by "npm run build" inside the image
dist

# misc
*.log
Dockerfile
.dockerignore
