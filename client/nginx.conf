server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: if the requested path isn't a real file, serve index.html
    # so React Router can handle routes like /products/123 on a page refresh.
    try_files $uri $uri/ /index.html;
  }
}
